### Test assignment objective
Implement functionality that can be used to summarize companies’ SDG alignment.

### Tech stack

The code is organized in a monorepo (using npm workspaces) with two packages: "client" and "server".

#### Client package tech stack:

- React
- Typescript
- Tanstack Query
- tRPC
- React Aria Components
- React Router
- Recharts
- 
#### Server package tech stack:

- Docker
- PostgreSQL
- tRPC
- Zod
- Typescript

### To run the app run the following commands:

 - `docker-compose up -d` - to start the database
 - `npm install` - to install all the packages
 - `npm run dev` - to start the development server

Then open "http://localhost:3000/" url in your browser.
